# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# GitHub recommends pinning actions to a commit SHA.
# To get a newer version, you will need to update the SHA.
# You can also reference a tag or branch, but the action may change without warning.

name: Deploy docs to Azure Static Web Apps

env:
  APP_LOCATION: "docs/swagger" # location of your client code

on:
  push:
    branches: [ main ]
    paths:
      - "docs/swagger/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v6
      - working-directory: docs/swagger
        run: |
          mv public.html public.temprename
          mkdir flatly
          cp flatly.html flatly/index.html
          cp flatly.yaml flatly/flatly.yaml
          mkdir fulldocs
          mv *.html fulldocs
          mv *.yaml fulldocs
          mv logo.svg fulldocs/logo.svg
          mv public.temprename index.html
      - name: Build And Deploy
        uses: Azure/static-web-apps-deploy@1a947af9992250f3bc2e68ad0754c0b0c11566c9
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_OFFICELY }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: ${{ env.APP_LOCATION }}
          skip_app_build: true
